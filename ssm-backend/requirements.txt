The system is meant to digitize operations such as:
- resident management
- visitor management
- facility booking
- maintenance billing 
- announcements 

architecture : asp.net core webapi -> service layer
               -> repository layer -> postgres


Requirements : 

1. Functional
- Add Delete Update Residents
    ~ Resident Management : who lives where 
- Visitor validation through QR(?)
    ~ Visitor Management : QR Validations, entry exit 
    logging/tracking, approvalse
    - Generated by residents for visitors - different
    for deliveries and others?
    - Indicates that the visitor is for x apartment,
    will be visiting between a and b time
    - system validates it - valid, expired, invalid, 
    already used?
    - How do you generate this and store it? - store 
    string to be encoded
    - Do I have to generate this everytime
    theres a visitor or would it be the same for 
    everyone(real life invalid scenario)
- Maintenance bill generation
- Payment tracking
- Booking conflicts - shared resources 
- Announcements
- Auth and Auth
- web apis, pagination, filtering etc

2. Non Functional
- Layered architecture

Architecture:
Angular Frontend → ASP.NET Core Web API → Service Layer →
Repository Layer → PostgreSQL Database
Entities : 


Domain and Repositories - done

Application layer :

dtos, services, interfaces